import { SwipeNoteDirective } from './swipe-note.directive';

describe('SwipeNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeNoteDirective();
    expect(directive).toBeTruthy();
  });
});
